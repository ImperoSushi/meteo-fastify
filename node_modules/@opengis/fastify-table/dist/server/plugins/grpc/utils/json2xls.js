import grpc from "../grpc.js";
const { jsonToXls } = grpc();
export default async function json2xls({ json }) {
    try {
        const result = await jsonToXls({ buffer: json });
        return { resultBuffer: result.result };
    }
    catch (err) {
        return { err: err.toString() };
    }
}
