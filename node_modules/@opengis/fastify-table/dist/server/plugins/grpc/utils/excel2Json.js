import grpc from "../grpc.js";
const { excelToJson } = grpc();
export default async function excel2Json({ buffer, type, delimiter }) {
    const { result } = await excelToJson({
        buffer: buffer.toString("base64"),
        type,
        delimiter,
    });
    return { resultBuffer: result };
}
