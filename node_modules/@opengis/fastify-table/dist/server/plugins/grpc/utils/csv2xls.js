import grpc from "../grpc.js";
const { csvToXls } = grpc();
export default async function csv2excel({ csvBuffer }) {
    const result = await csvToXls({ buffer: csvBuffer.toString("base64") });
    return { resultBuffer: result.result };
}
